---
import SidebarNav from '../components/SidebarNav.astro';
import MobileTopNav from '../components/MobileTopNav.astro';
import MainContent from '../layouts/MainContent.astro';
import { ViewTransitions } from 'astro:transitions';
import { getEntry } from 'astro:content';
import '../styles/global.css';

// Get the resume page content from Astro content collections
const resumePage = await getEntry('pages', 'resume');
const { Content: ResumeContent } = resumePage ? await resumePage.render() : { Content: null };
---

<!DOCTYPE html>
<html lang="en">
  <head>
  <meta charset="UTF-8" />
  <meta name="description" content="Resume for Justin Johnson, creative technologist and sound designer" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="Permissions-Policy" content="encrypted-media=(*)" />
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link rel="apple-touch-icon" href="/favicon.ico" />
  <title>Resume - Justin Johnson</title>
    <ViewTransitions />
  </head>
  <body class="text-black overflow-x-hidden bg-white">
    <!-- Mobile Top Navigation -->
    <MobileTopNav />

    <!-- Sidebar Navigation -->
    <SidebarNav />

    <!-- Main Content Area -->
    <MainContent>
      <!-- Resume page content -->
      {ResumeContent && (
        <div class="p-4 md:p-6 lg:p-8 prose prose-lg max-w-none">
          <ResumeContent />
        </div>
      )}
    </MainContent>
  </body>
</html>
